import { extendTheme } from '@chakra-ui/react';
import colors from './config/colors';
import fonts from './config/fonts';
import styles from './config/styles';

export const custonTheme = extendTheme({
  colors,
  fonts,
  styles,
});
